
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
# Added by code-review-graph
.code-review-graph/

# Personal dev configs (not published with the floreo plugin)
.claude/skills/
.claude/settings.json
.mcp.json
.opencode.json

# Build / planning artifacts (not published with the floreo plugin)
docs/baseline/project-proposal-baseline.html
docs/baseline/retro-baseline.html
docs/floreo-build-summary-*.html

# SkillSpector local scan reports (CI uploads them as artifacts instead)
skillspector-reports/

# System files
.DS_Store
